services:
  portal:
    image: nginx:alpine
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./build/client:/usr/share/nginx/html:ro
    restart: always